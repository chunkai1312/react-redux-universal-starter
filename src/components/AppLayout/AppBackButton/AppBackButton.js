import React from 'react'
import { IconButton } from 'react-toolbox/lib/button'

const AppMenuButton = (props) => (
  <IconButton {...props} icon="arrow_back" inverse />
)

export default AppMenuButton
