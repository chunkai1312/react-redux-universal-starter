import React from 'react'
import { Layout } from 'react-toolbox/lib/layout'
import theme from './theme.scss'

const AppLayout = (props) => <Layout {...props} theme={theme} />

export default AppLayout
